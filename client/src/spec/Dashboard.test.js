import React from 'react';
import { render, screen } from '@testing-library/react';
import Dashboard from '../components/Dashboard';

// const server = setupServer(
//   router.get(`/items/${Dashboard.menu._id}`, (req, res, ctx) => {
//     return res(ctx.json({ _id: String, name: String, price: Number, outOfStock: Boolean }));
//   }),
// );

// beforeEach(() => render(<Dashboard />));

// test('renders Dashboard', () => {
//   const linkElement = screen.getByText(/Menu/i);
//   expect(linkElement).toBeInTheDocument();
// });

// test('loads menu', async () => {});
test('', () => {

});